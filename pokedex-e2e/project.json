{
  "name": "pokedex-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "pokedex-e2e/src",
  "targets": {
    "e2e": {
      "executor": "@nx/cypress:cypress",
      "options": {
        "cypressConfig": "pokedex-e2e/cypress.config.ts",
        "testingType": "e2e",
        "devServerTarget": "pokedex:serve"
      },
      "configurations": {
        "production": {
          "devServerTarget": "pokedex:serve:production"
        },
        "ci": {
          "devServerTarget": "pokedex:serve-static"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["pokedex-e2e/**/*.{js,ts}"]
      }
    }
  },
  "implicitDependencies": ["pokedex"],
  "tags": []
}
